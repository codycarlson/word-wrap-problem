import spock.lang.Specification

class WordWrapperTest extends Specification {

    def "Insert meaningful test name here"() {
        given:
        //set up a thing
        when:
        //we do a thing
        then:
        true
        //the thing that we thought would happen, happened
    }

}
